VITE_API_URL=http://localhost:3001
# Client origin for course share links + QR codes. Set this in local dev where the
# client runs on a different port. Leave UNSET in production — admin and client are
# same-origin there, so it falls back to window.location.origin automatically.
VITE_CLIENT_URL=http://localhost:5174
